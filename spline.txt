Spline 3D Integration Plan
--------------------------
Target Section: "Why This Matters Right Now" cards (in src/components/WelcomePage.tsx)
Spline Asset URL: https://prod.spline.design/IYMKvgqnK80K9V2W/scene.splinecode

What we planned to do:
- Install `@splinetool/react-spline` and `@splinetool/runtime` packages via pnpm.
- Add the `Spline` component as an absolute background layer to the 3 cards in the "Why This Matters" section.
- Apply opacity transition on hover to make the 3D asset fully visible when the user hovers over a card.
- Make the `Spline` canvas interactive (`pointer-events-auto`) while making the text overlay `pointer-events-none` so the user can interact with the 3D element.

Code to add in `WelcomePage.tsx`:
1. Import statement:
```tsx
import Spline from '@splinetool/react-spline/next';
```

2. Card JSX structure:
```tsx
<div key={i} className="relative flex flex-col p-7 rounded-[14px] bg-white/[0.03] border border-white/[0.08] hover:border-white/[0.14] transition-all duration-500 group overflow-hidden">
  {/* Spline 3D Asset */}
  <div className="absolute -top-10 -right-10 w-[240px] h-[240px] opacity-20 group-hover:opacity-100 transition-all duration-700 z-0 hover:scale-110 pointer-events-auto mix-blend-screen">
    <Spline scene="https://prod.spline.design/IYMKvgqnK80K9V2W/scene.splinecode" />
  </div>

  <div className="relative z-10 flex flex-col h-full pointer-events-none">
    <div className="w-12 h-12 rounded-xl bg-violet-500/10 border border-violet-500/20 flex items-center justify-center text-violet-400 mb-6 group-hover:scale-105 transition-transform shrink-0">
      <IconComp className="w-5 h-5" />
    </div>
    <span className="font-mono text-[11px] text-white/40 tracking-widest uppercase block mb-2">
      {c.tag}
    </span>
    <div className="text-[32px] font-semibold mb-3 bg-gradient-to-br from-white to-white/70 bg-clip-text text-transparent tracking-tight leading-none">
      {c.stat}
    </div>
    <p className="text-[#A6A6B3] text-[15px] leading-relaxed mb-5 flex-grow">
      {c.body}
    </p>
    <span className="font-mono text-[11px] text-white/30 italic">
      {c.src}
    </span>
  </div>
</div>
```

Why removed temporarily:
The Spline package installation/loading was taking too long, hindering development flow. We removed it so development on other features can continue smoothly. Once the packages are properly installed and performance verified, we can re-add the code snippet above.
